## Question
Which v2 design decisions (message kinds, supervised-agent mode, coordinator behavior) carry over, and what exactly made the client integration unreliable and let subagents poke Daniel directly?
